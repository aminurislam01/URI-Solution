#include<stdio.h>

int main()
{
	int N[100],i;
	double a,x,j;
	scanf("%lf",&a);
	for(i=0,j=a;i<100;i++,j=j/2)
	{
		//x=a;
		 printf("N[%d] = %0.4lf\n",i,j);
	    // N[0] = 200.0000
	    // a=a/2;
	   //  if(i==1000)
	     //   return i;

	}
	return 0;
}
